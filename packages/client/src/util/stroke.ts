import { PALETTE } from './palette';

export const MIN_STROKE_THICKNESS = 1;
export const MAX_STROKE_THICKNESS = 100;
export const DEFAULT_STROKE_THICKNESS = 20;
export const DEFAULT_STROKE_COLOR = PALETTE[0];
